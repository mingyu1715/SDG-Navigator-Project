{
  "name": "sdg-circular-cards",
  "version": "1.0.0",
  "private": true,
  "description": "SDG circular card UI (static)",
  "scripts": {
    "start": "echo 'Server running at http://localhost:3000' && python3 -m http.server 3000"
  }
}
